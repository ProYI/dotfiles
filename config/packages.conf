# Dotfiles 基础软件包清单
# 格式：分类:逻辑包名 逻辑包名 ...
# base  适合新机器默认安装，保证 dotfiles 基础体验可用。
# extra 增强工具，需要时通过 ./install.sh --packages base,extra 安装。

base:git curl wget ca-certificates zsh vim tmux fzf ripgrep fd bat jq unzip zip tree
extra:build-tools htop zoxide xclip wl-clipboard net-tools
